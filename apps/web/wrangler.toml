# =============================================================================
# Cloudflare Pages configuration (auto-detected by Cloudflare from wrangler.toml)
# =============================================================================
# Env vars (NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY) are set
# in Cloudflare Dashboard > Pages > Settings > Environment variables
# (NOT in this file, because Pages wrangler.toml doesn't support [vars]).
#
# Compatibility flags (nodejs_compat) and compatibility date are configured
# in Dashboard > Settings > Functions.

name = "inventorypro-web"
compatibility_date = "2024-09-23"
pages_build_output_dir = ".vercel/output/static"
